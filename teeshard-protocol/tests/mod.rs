// teeshard-protocol/tests/mod.rs

// Optionally declare other test modules if they exist
// mod scenario_a_shard_scalability; // No need to declare tests run by cargo

// Declare other test modules to help with path resolution
mod scenario_a_shard_scalability;
mod scenario_b_cross_chain_ratio;
mod scenario_d_network_delay;
mod scenario_f_single_vs_multi_chain;
// Add others if needed, like full_protocol_e2e_test, simulation_tests etc.
// mod full_protocol_e2e_test;
// mod simulation_tests;
// mod e2e_swap_test;

// Optionally declare other test modules if they exist
// mod scenario_a_shard_scalability; // No need to declare tests run by cargo 